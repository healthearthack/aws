name: Deploy Pipeline

on:
  push:
    branches: [main]

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Validate tokens.json
        run: |
          echo "Validating design tokens..."
          python3 -m json.tool "My Design System/Full color token system/tokens.json"
          echo "Tokens valid ✅"

      - name: Show enterprise structure
        run: |
          echo "Enterprise folders:"
          ls enterprises/
          echo "Design system:"
          ls "My Design System/"