name: Deploy Pipeline

on:
  push:
    branches: [main]

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Validate Design System
        run: |
          echo "Checking tokens.json..."
          python3 -m json.tool "My Design System/Full color token system/tokens.json"
          echo "Design system valid ✅"

      - name: Confirm Enterprise Structure
        run: |
          echo "Enterprise folders:"
          ls enterprises/
          echo "Design system folders:"
          ls "My Design System/"r